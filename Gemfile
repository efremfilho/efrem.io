source "https://rubygems.org"

# Mesmo ambiente usado pelo GitHub Pages (Jekyll 3.10 + plugins permitidos,
# incluindo jekyll-redirect-from). Para testar localmente:
#   bundle install
#   bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick"
